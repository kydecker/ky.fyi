---
import Expandable from "../Expandable.astro";
import ProjectCard from "./ProjectCard.astro";

interface Props {
  items: Array<{
    id: string;
    title: string;
    description?: string;
    url: string;
    category: string;
    year: number;
    previewImage?: {
      image: ImageMetadata;
      alt: string;
    };
  }>;
  cutoff?: number;
}

const { items, cutoff } = Astro.props;
const visible = cutoff ? items.slice(0, cutoff) : items;
const hidden = cutoff ? items.slice(cutoff) : [];
---

<Expandable layout="grid" count={hidden.length}>
  <Fragment slot="visible">
    {visible.map((item) => <ProjectCard item={item} />)}
  </Fragment>
  {hidden.map((item) => <ProjectCard item={item} />)}
</Expandable>
